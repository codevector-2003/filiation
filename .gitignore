# Go
/fil
/fil.exe
/dist/
*.test
*.out
vendor/

# Tooling
.golangci-cache/
coverage.out

# Filiation data — never commit a user's library
*.db
*.db-wal
*.db-shm
/data/
/papers/
/blobs/
.filiation/

# Web UI build output is embedded at build time, not committed
internal/web/dist/

# Secrets
.env
.env.*
!.env.example

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Source art for the README banner and social preview (large; the composed
# docs/assets/banner.jpg and social-preview.png are what is committed).
docs/assets/*.jfif
